# Knowledge Platform Clients

Service-owned clients for infrastructure such as Redis, Qdrant, MinIO, parser
workers, and embedding providers will live in this directory.

Do not expose raw infrastructure payloads or credentials through HTTP handlers.
