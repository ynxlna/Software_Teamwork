# Parser Service Package

Python parser runtime code belongs under this package.

The implementation should expose the internal HTTP contract documented in
`services/parser/api/openapi.yaml` and keep PaddleOCR runtime dependencies out
of Knowledge.
